{% props data, mailingForm %}
<div{{ attributes }}>

    <!-- Mailing List Section -->
    <section id="subscribe" class="h-screen pt-20 bg-white flex items-center">
        <div class="container mx-auto px-6 text-center max-w-lg">
            <h2 class="text-3xl font-bold mb-10">Join Our Mailing List</h2>
            <p class="mb-10 text-gray-600 text-lg text-center">
                {{ data.description }}
            </p>
            {{ include('partial/_newsletter-form.html.twig') }}
        </div>
    </section>
</div>
