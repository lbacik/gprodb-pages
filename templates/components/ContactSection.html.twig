{% props data, contactForm %}
<div{{ attributes }}>
    <!-- Contact Section -->
    <section id="contact" class="h-screen pt-20 bg-gray-100 flex items-center">
        <div class="flex flex-col items-center justify-center w-full space-y-10">
            <div class="container mx-auto px-6 max-w-lg">
                    <div class="text-lg mb-8 text-center">
                        {{ data.description|markdown_to_html }}
                    </div>
                    {{ include('partial/_contact-form.html.twig') }}
            </div>
        </div>
    </section>
</div>
